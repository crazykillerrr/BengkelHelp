import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF0066CC);        // Biru utama
  static const Color secondary = Color(0xFFFFA726);      // Oranye lembut
  static const Color error = Color(0xFFD32F2F);          // Merah error

  static const Color background = Color(0xFFF5F5F5);     // Abu muda
  static const Color surface = Color(0xFFFFFFFF);        // Putih

  // Text Colors
  static const Color textBlack = Color(0xFF212121);
  static const Color textGrey = Color(0xFF757575);
  static const Color textWhite = Color(0xFFFFFFFF);
}
